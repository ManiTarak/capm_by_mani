{
  // uncomment entries once all libraries have been installed via 'npm install'
  "eslint.validate": [
    // "cds",
    // "csn",
    // "csv",
    // "csv (semicolon)",
    // "tab",
    // "tsv"
  ],
  "sqltools.connections": [
    {
      "previewLimit": 50,
      "driver": "SQLite",
      "name": "enterprise_procurement_model",
      "database": "${workspaceFolder:capm2}/db.sqlite"
    }
  ]
}
